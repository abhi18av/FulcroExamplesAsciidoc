version: '1'

tasks:
  html:
    cmds:
      - asciidoctor -o DevelopersGuide.html -b html5 -r asciidoctor-diagram -r ./register-fulcro-examples.rb DevelopersGuide.adoc

  extension:
    cmds:
      - asciidoctor -r asciidoctor-diagram -r ./register-fulcro-examples.rb DevelopersGuide.adoc

  expanded:
    cmds:
      - asciidoctor -o ExpandedSource.html -b html5 ExpandedSource.adoc
